<%- include('templates/header', { title: 'Admin Panel' }) %>

<header class="bg-dark text-white text-center py-4 mb-4">
    <h1>Admin Panel</h1>
</header>

<main class="container">
    <ul class="list-group">
        <% users.forEach(user => { %>
            <%- include('templates/user', { user }) %>
        <% }) %>
    </ul>
</main>

<%- include('templates/footer') %>
